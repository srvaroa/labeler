name: Label Pull Requests

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Label PRs
      run: |
        go build -o action ./cmd
        ./action